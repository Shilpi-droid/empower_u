
import 'package:flutter/material.dart';

class Mute extends StatelessWidget {
  const Mute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
